apiVersion: vault.platform.io/v1alpha1
kind: VaultClusterRole
metadata:
  labels:
    app.kubernetes.io/name: vault-access-operator
    app.kubernetes.io/managed-by: kustomize
  name: vaultclusterrole-sample
spec:
  # TODO(user): Add fields here
